[project]
name = "keystone"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "masonite-framework>=5.0,<6",
    "masonite-framework-orm>=3.0,<4",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.1",
    "pytest>=9.1.1",
    "ruff>=0.16.1",
]

[tool.ruff.lint]
ignore = ["N999"]

